spec:
  data:
    app:
      name: example-app
    configmap:
      data: 'ENV: null'
    image:
      repository: nexus.com.br
      tag: 'null'
    resources:
      limits:
        cpu: 512m
      requests:
        cpu: 256m
  enable: true
  ingress:
    additional:
      path: /example-app
