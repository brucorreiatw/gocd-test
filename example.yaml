spec:
  data:
    app:
      name: example-app
    configmap:
      data: 'ENV: prod'
    image:
      repository: nexus.com.br
      tag: 1.0.1
    resources:
      limits:
        cpu: 512m
      requests:
        cpu: 256m
  enable: true
  ingress:
    additional:
      path: /example-app
